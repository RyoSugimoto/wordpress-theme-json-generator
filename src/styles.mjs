import body from './styles/body.mjs'
import blocks from './styles/blocks.mjs'
import elements from './styles/elements.mjs'

/**
 * styles
 * サイト全体や要素、ブロックにスタイルを適用するための設定。
 */
export default {
  ...body,
  blocks,
  elements,
}
